<section class="relative min-h-screen min-h-[100svh] w-full text-white overflow-hidden">
<div class="flex items-center justify-center pt-28 md:pt-32">

  <!-- BACKGROUND IMAGE -->
  <img 
    src="/bg-hero.jpg" 
    alt="FunGab Studio" 
    class="absolute inset-0 w-full h-full object-cover"
  />

  <!-- GRADIENT OVERLAY -->
 <div class="absolute inset-0 bg-gradient-to-r 
  from-black/70 
  via-black/60 
  to-black/40">
</div>


  <!-- CONTENT -->
  <div class="relative text-center px-6 animate-fadeIn">
    <h1 class="text-4xl md:text-6xl font-bold mb-4 leading-tight">
      Where Ideas Become Impact
    </h1>
    <h2 class="text-4xl  font-bold mb-4 leading-tight">
      A UK-Based Creative Powerhouse
    </h2>
    <br>


    <p class="text-lg md:text-xl max-w-2xl mx-auto mb-8">
      We help brands, artists, and businesses transform ideas into powerful media, 
      immersive sound, compelling visuals, and results-driven creative 
      experiences thereby Bringing Vision to Life — all delivered with precision and purpose.
    </p>

    <!-- CTA BUTTONS -->
    <div class="flex flex-row gap-3 justify-center flex-wrap">
      <a 
        href="/booking"
        class="px-5 py-2 rounded-full bg-gradient-to-r from-[#f1a0ec] to-[#baecfe] text-black font-semibold hover:bg-gray-200 transition"
      >
        Start a Project
      </a>

      <button 
        class=" px-4 py-2 rounded-full border-2 border-white flex items-center justify-center hover:bg-white/20 transition"
        onclick="alert('Play demo functionality will be added later!')"
      >
        View
      </button>
    </div>
  </div>

  <a
  href="#next-section"
  class="scroll-indicator"
  aria-label="Scroll to next section"
>
  <span></span>
</a>
</div>


</section>


<style>
@keyframes fadeIn {
  from { opacity: 0; transform: translateY(20px); }
  to { opacity: 1; transform: translateY(0); }
}
.animate-fadeIn {
  animation: fadeIn 1.4s ease-out forwards;
}

html {
  scroll-behavior: smooth;
}

/* Scroll Down Indicator */
.scroll-indicator {
  position: absolute;
  bottom: 2rem;
  left: 50%;
  transform: translateX(-50%);
  width: 28px;
  height: 48px;
  border: 2px solid rgba(255, 255, 255, 0.8);
  border-radius: 20px;
  display: flex;
  justify-content: center;
  padding-top: 6px;
  cursor: pointer;
  transition: opacity 0.3s ease;
  z-index: 20;
}

.scroll-indicator:hover {
  opacity: 0.7;
}

.scroll-indicator span {
  width: 6px;
  height: 6px;
  background: white;
  border-radius: 50%;
  animation: scrollDown 1.6s infinite;
}

@keyframes scrollDown {
  0% {
    transform: translateY(0);
    opacity: 1;
  }
  50% {
    transform: translateY(10px);
    opacity: 0.6;
  }
  100% {
    transform: translateY(18px);
    opacity: 0;
  }
}

</style>
